build:
  gpu: true
  python_version: "3.11"  # support only 3.11
  python_packages:
    - torch==2.2.1
    - torchvision==0.17.1
    - opencv-contrib-python-headless==4.8.1.78
    - onnxruntime==1.17.1
repo: admin/yolov7-gpu  # make sure the format is {user-id}/{model-id} and the
tag: rev1               # {model-id} match the model namespace created on console
